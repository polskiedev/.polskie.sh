{
    "commands": [
        {
            "command": "list",
            "alias": "ls",
            "description": "List files"
        },
        {
            "command": "cd:home",
            "alias": "cd $HOME",
            "description": "Go to Home Directory"
        },
        {
            "command": "lc",
            "alias": "to_lowercase_test",
            "description": "Convert string to lowercase"
        }
    ]
}
